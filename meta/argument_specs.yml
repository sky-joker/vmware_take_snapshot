---
argument_specs:
  main:
    short_description: "The variables are used in running the role"
    options:
      datacenter_name:
        type: str
        required: true
        description: "The datacenter name is that has vm"
      vm_folder:
        type: str
        required: false
        description: "The vm_foler is folder name that has vm"
      domain:
        type: str
        required: false
        description: "The domain for using in vm"
      take_snapshot:
        type: str
        required: true
        description: "The take_snapshot is a flag whether taking a new snapshot"
